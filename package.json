{
  "name": "pku-hole-toolkit",
  "version": "1.3.0-beta.3",
  "private": true,
  "type": "module",
  "description": "Local-first PKU Treehole export, migration, and archive toolkit",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "lint": "node scripts/lint.mjs",
    "format:check": "node scripts/format-check.mjs",
    "inspect:archive": "node scripts/inspect-archive.mjs",
    "check": "npm run lint && npm run format:check && npm run test && npm run build && node --check \"PKU-Hole export tool.user.js\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
