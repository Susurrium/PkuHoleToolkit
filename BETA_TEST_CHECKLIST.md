# v1.3.0 Beta 验收记录

验收日期：2026-07-11 至 2026-07-12
候选版本：`v1.3.0-beta.4`
浏览器：Microsoft Edge + Tampermonkey
真实数据范围：收藏分组“测试组”（2 个主洞，其中 1 个正文引用组外洞）

整个验收只允许读取和本地归档；没有执行新增关注、取消关注、发帖或评论操作。测试记录不保存洞正文、评论、PID、token、Cookie 或 UUID。

## 真实环境通过项

### 页面兼容性

- [x] 页面正常加载，工具栏只出现一个“归档/迁移”入口。
- [x] 面板可以打开、关闭并切换导出/导入页签。
- [x] 修复 `beta.1` 的 `MutationObserver` 自激白屏回归。
- [x] Edge 冒烟夹具正常结束，入口和 Shadow DOM 宿主各只有一个。

### 基础与无评论导出

- [x] 按收藏分组正确读取 2 个主洞。
- [x] 关闭评论和引用后得到 2 个 `followed`、0 个 `referenced`、0 条评论。
- [x] manifest 为 `complete: true`，无失败、无重复 PID。

### 评论与一层引用

- [x] 开启评论及正文/评论引用后，完整导出 218 条评论。
- [x] 正确识别关注接口规范化后的正文开头 PID。
- [x] 得到 2 个 `followed` 和 1 个组外 `referenced`。
- [x] 引用 PID 去重，组内已有 PID 不会重复写入归档。

### 归档完整性与隐私

- [x] v2 ZIP 可通过 CRC、manifest、数据结构及数量一致性检查。
- [x] `uniquePids` 与导出条目数一致。
- [x] `sensitiveKeyCount: 0`。
- [x] manifest 不包含 `accountFingerprint`。
- [x] 未在日志、Issue 或验收记录中输出洞内容或账号凭据。

### 只读导入预检

- [x] 成功解析 1 个 v2 ZIP。
- [x] 正确识别 2 个唯一 PID、0 个待新增、2 个已关注。
- [x] 重复记录为 0，无效文件/记录为 0。
- [x] “确认导入”保持禁用，没有执行关注写请求。
- [x] `beta.4` 显示归档解析和远程关注分页进度。
- [x] `referenced` 仅作为上下文归档，不进入待关注 PID。
- [x] 远程关注分页不完整时，UI 和任务层均禁止写入。

## 自动化验证

- [x] 25 项 Node 自动化测试通过。
- [x] 2000 洞模拟导出通过，零回复洞不会请求评论接口。
- [x] 暂停/恢复不会重复已完成项目。
- [x] GET 分类重试、401 停止、二次 429 暂停通过。
- [x] 关注 POST 不自动重试，响应不确定时只做最终 GET 核对。
- [x] GitHub Actions CI 通过。

## 未执行的真实账号操作

- [ ] 真实网站中的暂停/刷新/恢复：测试组任务完成过快；由自动化测试覆盖。
- [ ] 实际新增关注：有账号状态变更风险，本轮明确不授权；由模拟 API 测试覆盖。
- [ ] 工具栏不存在时的 10 秒浮动入口：由 Edge 冒烟夹具和静态测试覆盖。

## 发布判断

核心只读归档、评论、引用、格式兼容、安全清理和导入预检均通过真实环境验收。实际写入仍受二次确认、单次 POST、最终状态读取核对和审计报告保护。未执行项必须继续在发布说明中披露，不得宣称已完成真实账号写入验证。
