# v1.3.0 Beta 验收清单

本清单用于真实树洞环境中的小范围只读验证。不得为了测试故意触发限流，也不得在未经单独授权时执行关注导入。

## 当前测试范围

- 浏览器：Microsoft Edge
- 数据范围：收藏分组“测试组”（4 个洞）
- 允许操作：页面挂载、读取收藏分组、导出正文/评论/一层引用、暂停与恢复、导入预览
- 禁止操作：实际关注、取消关注、修改或发布任何树洞内容

## 安装前

- [ ] 已登录 `https://treehole.pku.edu.cn/web/`
- [ ] 已禁用旧版同名脚本，避免重复入口和重复任务
- [ ] 安装的脚本头部版本为 `1.3.0-beta.1`
- [ ] GitHub CI 已通过，Release 附件哈希与本地构建一致

## A. 页面兼容性

- [ ] 页面工具栏只出现一个“归档/迁移”入口
- [ ] 找不到工具栏时，10 秒后只出现一个右下角备用入口
- [ ] 打开、关闭、重新打开面板均正常
- [ ] 面板不遮挡网站主要操作，键盘焦点和状态提示可用

## B. 四洞只读导出

### B1. 正文

- [ ] 范围选择“收藏分组”并选择“测试组”
- [ ] 关闭评论和引用，完成导出
- [ ] 只产生一个 `.treehole.zip`
- [ ] manifest 显示 `complete: true`、`exportedHoles: 4`、`failed: 0`

### B2. 评论

- [ ] 对同一分组开启评论后再次导出
- [ ] 评论数为 0 的洞没有额外评论分页请求
- [ ] ZIP 中仍只有 4 个唯一主洞，评论统计与数据一致

### B3. 一层引用

- [ ] 开启一层 `#PID` 引用后导出
- [ ] 引用 PID 去重，且不会递归抓取第二层引用
- [ ] 若没有引用，结果仍正常完成

## C. 恢复能力

- [ ] 任务开始后立即暂停；若四洞任务完成过快，则记录为“不适用”
- [ ] 刷新页面后能发现暂停任务
- [ ] 恢复后已完成 PID 不重复获取
- [ ] 取消任务不会继续发送请求

## D. 归档安全检查

对每个导出文件运行：

```powershell
npm run inspect:archive -- "<下载文件路径>" --expected=4
```

- [ ] 输出 `valid: true`
- [ ] 输出 `uniquePids: 4`
- [ ] 输出 `sensitiveKeyCount: 0`
- [ ] 不在终端、Issue 或测试记录中粘贴洞正文、评论、token、Cookie 或 UUID

## E. 导入预览

- [ ] 选择刚导出的 ZIP，只进入预览
- [ ] 预览数量正确，重复 PID 已去重
- [ ] 在最终确认前退出，不发送关注 POST

## 通过标准

- 所有适用项目通过；没有凭据泄露、重复入口、重复 PID 或非预期写请求。
- 归档完整性和数量检查通过。
- 任何失败均先记录复现步骤并停止扩大测试范围。

通过后才能创建正式 `v1.3.0`；失败则修复并发布 `v1.3.0-beta.2`。
