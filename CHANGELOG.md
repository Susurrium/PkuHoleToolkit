# Changelog

## Unreleased

## 1.4.1 - 2026-08-09

- 重构 Toolkit 界面信息架构与文案：突出可独立完成的本地备份和关注迁移，按运行状态呈现任务操作，将最近备份从 Studio 管理中分离，并把 PkuHoleStudio 收纳为可选联动。
- 修复导入任务的失败/未知项重试：已成功 PID 保持跳过，未完整 PID 先核对当前状态再安全重试，新结果按 PID 覆盖旧结果，不重复累计审计数量。
- 导入预检和长任务绑定账号指纹；登录 token 或 UUID 变化时刷新凭据并在下一次请求前停止旧账号任务，界面也会清空旧账号的归档、预检与断点。旧版断点仍可在原账号恢复。
- 修复刷新后遗留的 `planning`/`running` 任务无法恢复、重复打开面板可重入恢复发现，以及新任务误复用旧断点等问题。
- 严格校验收藏分组、PID 和日期导出范围；日期按浏览器本地自然日的闭区间筛选，并支持单边开放范围。
- 取消、忘记或重新发起 Studio 关联时会中止并等待旧轮询收尾；手动刷新也统一走可取消 watcher，延迟落盘的旧批准无法“复活”或覆盖新请求。
- 将 lockfile 中的内置 Archive Contract 版本与 `2.1.0` 同步。

## 1.4.0 - 2026-07-26

- 归档生成后可独立选择“下载到本机”与“发送到已关联 Studio”，也可同时执行；默认继续下载到本机，未选择 Studio 时不会探测本机端口。
- 下载与 Studio 传输复用同一份已生成归档；Studio 未关联或发送失败不会把已经完成的抓取误报为失败，最近归档仍可重新下载或发送。
- archive v2 接入中立的 PkuHole Archive Contract 2.1.0：新归档携带 `specVersion`、`producer` 和直接可移植的 scope；旧 v2 继续兼容。
- Toolkit 使用与 Studio 相同的有效/无效 fixtures 执行契约测试，并拒绝无效时间、错误记录形状和未知必需扩展。
- 向 Studio 发送前会读取其原生归档能力，核对 schema、必需扩展和文件大小；不兼容时在签名和上传前终止。
- 能力结果按 Studio 实例缓存五分钟；新增双方真实导出 ZIP 的互操作测试，并将 Archive 2.1 的 ZIP 写入时间统一为 UTC、压缩方法固定为 STORE。
- PkuHoleStudio 本地桥接升级为“一次关联、可撤销信任”：设备私钥仅保存在用户脚本私有存储，Studio 只保存公钥；每次归档仍使用短时、一次性且绑定 SHA-256 的签名传输票据。
- Toolkit 刷新后可从 IndexedDB 恢复最近完成或部分完成的导出，直接重发到已关联 Studio 或重新下载，不必重新抓取树洞。
- Studio 收到归档后仍先预检、再由用户确认导入；旧版一次性接收码保留为兼容入口，等待上传 15 分钟、确认窗口 30 分钟。
- 桥接只向 `127.0.0.1` 发送归档文件，不发送树洞账号、Cookie、token 或 UUID。

## 1.3.0 - 2026-07-12

- 完成收藏分组、评论、一层组外引用、无评论归档和只读导入预检的真实环境验收。
- 修复 Beta 期间发现的入口观察器白屏、接口规范化引用漏检和预检无进度反馈问题。
- 25 项自动化测试、2000 洞模拟、Edge 冒烟测试与 GitHub CI 通过。
- 实际新增关注未在真实账号执行，继续由二次确认、单次 POST、最终 GET 核对和审计报告保护。

## 1.3.0-beta.4 - 2026-07-12

- 为导入预检增加归档解析和远程关注分页进度，避免长任务看起来无响应。
- 预检现在可以取消；没有待新增 PID 时保持“确认导入”禁用。

## 1.3.0-beta.3 - 2026-07-11

- 兼容 `/follow_v2` 将正文开头 `#PID` 规范化为无 `#` 的 `PID + 空格` 格式。
- 保持仅识别正文开头的裸 PID，避免把正文中普通长数字误判为引用洞。

## 1.3.0-beta.2 - 2026-07-11

- 修复入口按钮被 `MutationObserver` 反复重插造成的主线程无限循环和页面白屏。
- 将入口重新定位合并到动画帧，并增加按钮挂载幂等回归测试。

## 1.3.0-beta.1 - 2026-07-11

- 将单文件源码重构为可测试的模块，并继续生成单文件用户脚本。
- 读取请求改为串行、限速、分类重试；关注 POST 不再自动重试，并核对最终状态。
- 增加严格 PID 校验、导入预检、审计报告和旧版 JSON 兼容。
- 增加全量、收藏分组、PID、日期范围导出，以及评论和一层引用选项。
- 导出改为带 manifest 的单个 v2 ZIP，并在 IndexedDB 中保存七天断点。
- 使用 Shadow DOM 任务面板，支持进度、暂停、继续、取消和刷新后恢复。

## 1.2.1 - 2026-06-27

- 初始导入/导出脚本基线。
