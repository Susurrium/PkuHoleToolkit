# 北大树洞个人客户端 —— 方案设计文档

| 字段 | 值 |
|---|---|
| 版本 | v0.1 |
| 日期 | 2026-06-27 |
| 状态 | 初稿 / 待评审 |
| 作者 | WindMan |
| 范围 | 自用 + 小圈子分享，不公开发行 |

---

## 0. TL;DR

构建一个 **Tauri + React + SQLite** 的桌面客户端，分五个阶段把"树洞用户体验"从「翻官方收藏夹」升级到「个人知识库 + 智能问答 + 完整官网替代」。

- **MVP**（Phase 1，2-3 周）：导入现有油猴脚本导出的 JSON，做出**多标签 + 全文搜索 + 笔记**的离线阅读器，立刻替代官方收藏夹
- **后期**（Phase 2-5）：内置同步 → 知识图谱 → 语义搜索 → 写操作完整替代
- **关键原则**：每个阶段独立可用，不必走完才有价值；先离线后在线；先读后写；本地数据自主、互不共享

---

## 1. 项目背景

### 1.1 起源
项目起点是一个已有的油猴脚本 [`PKU-Hole export tool.user.js`](./PKU-Hole%20export%20tool.user.js)，提供关注列表的导入/导出功能。当前已能：

- 导出关注的所有洞 + 评论为 JSON / TXT
- 模式 2/3 时连带导出被引用的洞
- 反向导入：从 JSON 批量关注洞

但这只是**数据搬运层**，无法解决日常使用中真正的痛点。

### 1.2 官网现存痛点

| 痛点 | 现状 | 影响 |
|---|---|---|
| 收藏分组弱 | 一个洞只能进一个分组，没有多标签 | 当一个洞既属于"考研"又属于"复试"时无法同时表达 |
| 分组操作低效 | 收藏后弹分组选项有时不出现、流程繁琐 | 高频操作的体验摩擦 |
| 搜索能力弱 | 必须精确关键词，无模糊匹配/语义搜索 | 想不起原话就找不到 |
| 无个人沉淀 | 看过的洞无法留批注、无回顾、无统计 | 优质内容易遗忘 |
| 数据不可控 | 洞被删/账号变动后内容彻底丢失 | 长期内容无保障 |

### 1.3 改进方向

把官网当作**数据源**，把自建客户端当作**沉淀层 + 增强阅读层**：

- 数据存本地、可控、可备份
- UI/交互按自己的需求重做
- 在数据层之上叠加官网做不到的：多标签、全文搜、笔记、引用图谱、语义问答

---

## 2. 用户与场景

### 2.1 用户画像
- **主用户**：作者本人，北大学生
- **次用户**：少量信任的朋友（< 10 人）
- **技术背景**：近于专业开发，可独立完成 Tauri / React / SQL 工程

### 2.2 共享模式
**只共享代码、不共享数据**。每个人在自己的电脑上独立部署、独立维护自己的本地数据库与 token，互相之间不可见。

> 这条原则直接否决了"自建后端代理 + 多用户共享"的产品形态。

### 2.3 使用场景
1. **日常归档**：把每天看到的好洞收下来、打多个标签、加批注
2. **回溯查阅**：几个月后想找"那条讲考研经验的洞"，靠模糊搜索/语义搜索找回
3. **主题整理**：用智能收藏夹动态聚合"近半年所有带'复试'标签且评论数 > 50 的洞"
4. **长期回顾**：年度统计、引用图谱、笔记反向链接构成的"私人树洞百科"
5. **（后期）完全替代**：刷洞、回复、关注全部走自建客户端

---

## 3. 目标与非目标

### 3.1 核心目标
- ✅ 数据**自主可控**，本地优先
- ✅ 体验**显著优于官网**（至少在收藏、搜索、回顾三个场景）
- ✅ 架构**支持渐进式扩展**到完整官网替代
- ✅ 朋友能**自部署**（开源代码 + Release 安装包）

### 3.2 显式不做（Non-goals）
- ❌ **不做后端、不共享数据**——任何形式的集中化都被排除
- ❌ **不做后台常驻轮询**——所有同步必须用户主动触发
- ❌ **不做移动端**（至少 v1.x）
- ❌ **不做自动登录**——token 由用户手动粘贴
- ❌ **不做匿名性反向破解**——任何识别洞主真实身份的功能都不开
- ❌ **不公开发行**——不上架商店、不在公网做服务

---

## 4. 产品形态选型

### 4.1 候选方案对比

| 方案 | 拿 token | 数据存储 | UI 自由度 | CORS | 跨设备 | 完整替代 | 评价 |
|---|---|---|---|---|---|---|---|
| **A. 油猴脚本** | 自动（搭便车） | localStorage / IndexedDB | 低（嵌官网） | 无问题 | 无 | ❌ 永远是寄生层 | 跟"独立产品"目标冲突 |
| **B. 纯前端 SPA** | 手动粘贴 | IndexedDB | 高 | **致命** | 无 | ❌ | CORS 是硬墙 |
| **C. Tauri 桌面 app** | 嵌 WebView 或手动 | SQLite | 高 | 无问题 | 文件同步可补 | ✅ | **最匹配** |
| **D. 前后端分离** | 用户粘到服务器 | 服务器 DB | 高 | 后端代理 | 天然 | ✅ | **政策/伦理风险大** |

### 4.2 最终选择：**C. Tauri 桌面应用**

理由：
1. **CORS 不受限**：原生 HTTP 客户端可直接调官方 API，前端到后端的所有路径都通畅
2. **数据自主**：SQLite 文件就在用户磁盘上，符合"只共享代码"原则
3. **形态最匹配长期愿景**：桌面 app 是做完整客户端最自然的载体
4. **工程指标好**：Tauri 比 Electron 体积小一个数量级、内存友好、启动快
5. **跨平台**：Win / macOS / Linux 同一代码

### 4.3 移动端为何缓做
- 树洞使用强度通常是"碎片化但短时段"，桌面归档 + 手机刷官网的分工已经能满足
- 移动端 = 重新做一份 UI + 鉴权方案 + 同步方案，工作量倍增
- 若未来强需要：导出 SQLite 到云盘 + 移动端只读 viewer，是低成本补救

---

## 5. 技术栈

| 层 | 选择 | 说明 |
|---|---|---|
| 应用壳 | **Tauri 2.x** | Rust 后端 + Webview 前端 |
| 前端框架 | **React 18 + TypeScript + Vite** | 生态最大、组件库丰富 |
| UI 组件 | **shadcn/ui + Tailwind CSS** | 复制粘贴式组件，深度可改 |
| 状态管理 | **TanStack Query**（服务态）+ **Zustand**（UI 态） | 异步与本地状态分治 |
| 路由 | **TanStack Router** 或 React Router v7 | 文件式路由优先 |
| 数据库 | **SQLite**（`tauri-plugin-sql`）| 多标签 / FTS5 / 后期向量一站满足 |
| 中文分词 | **jieba-rs** + FTS5 tokenizer | FTS5 默认中文分词差，必须接外部 |
| 富文本/Markdown | **TipTap** 或 **CodeMirror 6 + remark** | 笔记编辑用 |
| 虚拟滚动 | **TanStack Virtual** | 几千条列表不卡 |
| 后期：向量 | **sqlite-vec** | 嵌入式向量索引 |
| 后期：LLM | **OpenAI 兼容协议** | 不绑死任何家，对接 OpenAI / DeepSeek / 智谱 / 本地 ollama |

---

## 6. 数据模型

### 6.1 设计原则
**"同步来的"和"本地产生的"严格分表**。重新同步永远不会覆盖你的笔记和标签。

### 6.2 表结构

```sql
-- ===== 同步来的（可被覆盖）=====
CREATE TABLE holes (
  pid          INTEGER PRIMARY KEY,
  text         TEXT NOT NULL,
  timestamp    INTEGER NOT NULL,
  likenum      INTEGER DEFAULT 0,
  reply        INTEGER DEFAULT 0,
  type         TEXT,            -- 'text' / 'image' / ...
  extra_json   TEXT             -- 其他官方字段原样存
);

CREATE TABLE comments (
  id           INTEGER PRIMARY KEY,
  pid          INTEGER NOT NULL REFERENCES holes(pid),
  name         TEXT,            -- 楼层名（Alice / 洞主 / ...）
  text         TEXT NOT NULL,
  timestamp    INTEGER NOT NULL,
  extra_json   TEXT
);
CREATE INDEX idx_comments_pid ON comments(pid);

CREATE TABLE hole_sync_meta (
  pid              INTEGER PRIMARY KEY REFERENCES holes(pid),
  last_synced_at   INTEGER NOT NULL,
  comment_count_seen INTEGER DEFAULT 0   -- 用于判断是否需要重拉评论
);

-- ===== 本地产生的（永不覆盖）=====
CREATE TABLE tags (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  name       TEXT NOT NULL UNIQUE,
  color      TEXT,
  parent_id  INTEGER REFERENCES tags(id),  -- 为未来层级标签预留
  created_at INTEGER NOT NULL
);

CREATE TABLE hole_tags (
  pid    INTEGER NOT NULL REFERENCES holes(pid),
  tag_id INTEGER NOT NULL REFERENCES tags(id),
  PRIMARY KEY (pid, tag_id)
);
CREATE INDEX idx_hole_tags_tag ON hole_tags(tag_id);

CREATE TABLE notes (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  pid        INTEGER REFERENCES holes(pid),   -- 可空：独立笔记
  content    TEXT NOT NULL,                   -- Markdown，可含 [[pid:123]]
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);
CREATE INDEX idx_notes_pid ON notes(pid);

CREATE TABLE collections (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT NOT NULL,
  query_json  TEXT,    -- 非空 = 智能收藏夹（动态查询）
  created_at  INTEGER NOT NULL
);

CREATE TABLE collection_holes (
  collection_id INTEGER REFERENCES collections(id),
  pid           INTEGER REFERENCES holes(pid),
  PRIMARY KEY (collection_id, pid)
);

CREATE TABLE hole_local_meta (
  pid       INTEGER PRIMARY KEY REFERENCES holes(pid),
  starred   INTEGER DEFAULT 0,
  read_at   INTEGER,
  archived  INTEGER DEFAULT 0
);

-- ===== 全文索引 =====
CREATE VIRTUAL TABLE holes_fts USING fts5(
  text, content='holes', tokenize='jieba'
);
CREATE VIRTUAL TABLE comments_fts USING fts5(
  text, content='comments', tokenize='jieba'
);

-- ===== Phase 4 才加 =====
CREATE TABLE hole_embeddings (
  pid        INTEGER PRIMARY KEY REFERENCES holes(pid),
  model      TEXT NOT NULL,        -- 记录哪个模型生成的
  vector     BLOB NOT NULL,
  created_at INTEGER NOT NULL
);

-- ===== Phase 5 才加 =====
CREATE TABLE write_drafts (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  type         TEXT NOT NULL,       -- 'new_hole' / 'reply'
  target_pid   INTEGER,             -- 回复时填
  content      TEXT NOT NULL,
  status       TEXT NOT NULL,       -- 'draft' / 'sending' / 'sent' / 'failed'
  idempotency_key TEXT UNIQUE,
  created_at   INTEGER NOT NULL,
  sent_at      INTEGER
);

CREATE TABLE sync_log (
  id        INTEGER PRIMARY KEY AUTOINCREMENT,
  action    TEXT NOT NULL,         -- 'pull_followed' / 'push_attention' / ...
  target    TEXT,
  status    TEXT NOT NULL,         -- 'ok' / 'fail' / 'partial'
  error     TEXT,
  ts        INTEGER NOT NULL
);
```

### 6.3 关键决策

1. **`pid` 作为洞的主键**——和油猴 JSON、官方 API 永远对齐，零转换成本
2. **`PRAGMA user_version` 从第一天就用**，每次改 schema 写迁移脚本
3. **`collections.query_json` 是"保存搜索"的载体**，例：
   ```json
   {
     "tags": ["考研", "复试"],
     "fts": "北邮",
     "date_after": "2024-01-01",
     "sort": "likenum_desc"
   }
   ```
4. **`tags.parent_id` 表结构预留树状**——MVP UI 不暴露，等需要时随时启用
5. **`notes.pid` 可空** + **`[[pid:123]]` 链接语法**——既可写洞批注、又可独立笔记反引用多条洞

---

## 7. 模块架构

```
src/
├── api/             # 树洞 API client（fetch 封装、token 管理、重试限速）
│   ├── client.ts    # 核心 fetch 封装
│   ├── endpoints.ts # followedHoles / comments / getHole / followHole / ...
│   └── token.ts     # token 读取与过期检测
├── sync/            # 同步引擎（Phase 1 留空）
│   ├── engine.ts    # 差分、增量、幂等
│   └── strategies/  # 全量、增量、单洞
├── db/              # SQLite 访问层
│   ├── migrations/  # 001_init.sql, 002_add_embeddings.sql, ...
│   ├── queries.ts   # 高层查询封装
│   └── schema.ts    # 类型定义
├── search/          # 搜索（FTS + 后期混合向量）
│   ├── fts.ts
│   └── hybrid.ts    # Phase 4
├── features/        # 业务模块
│   ├── holes/       # 列表、详情、引用图
│   ├── tags/        # 标签管理
│   ├── notes/       # 笔记
│   ├── collections/ # 智能收藏夹
│   ├── sync/        # 同步 UI
│   └── stats/       # 统计页
├── ui/              # 共用组件
└── app/             # 路由、布局、设置
```

**为什么强调这个分层**：所有外部副作用（HTTP、SQL）都在 `api/`、`db/`、`sync/` 三个目录里。UI 组件**只调 `features/*/hooks`**，不直接 fetch 也不直接写 SQL。这样：

- Phase 1 没有 API 调用，UI 组件依然能跑（只走 db/）
- Phase 2 加 API + sync，不动 UI
- Phase 4 加向量搜索，只动 search/，不动 UI

---

## 8. 五阶段路线图

### Phase 1 — 个人归档阅读器（MVP）

| 项 | 说明 |
|---|---|
| **预估** | 2-3 周（业余） |
| **目标** | 完全替代官方收藏夹的查阅、整理、备注场景 |
| **数据源** | 现有油猴脚本导出的 JSON |

**任务清单**：
- [ ] Tauri + React + TS 项目骨架
- [ ] SQLite 接入 + migration_001 + 类型生成
- [ ] **导入器**：兼容 `{holes: [], comments: []}` 格式，支持多文件、去重、增量
- [ ] **洞列表**：虚拟滚动；按时间/点赞/回复排序；翻页/分页加载
- [ ] **洞详情**：洞主 + 评论树；`#pid` 自动变可点击链接（本地有的洞内跳转）
- [ ] **标签**：多标签、批量打标签、标签管理 CRUD、按标签筛选
- [ ] **全文搜索**：FTS5 + jieba；分洞文/评论两个范围；命中高亮
- [ ] **笔记**：每洞下 Markdown 编辑器；自动保存
- [ ] **设置页**：DB 文件位置、备份、整库导出
- [ ] **可选**：智能收藏夹 v1（保存搜索条件）、暗色主题、图片本地缓存

**完成判据**：作者本人能彻底放弃官方收藏夹翻阅历史洞。

**风险**：
- 中文 FTS5 分词器接入有坑（jieba-rs 在 SQLite 端注册 tokenizer 需要写 C 接口或 Rust 桥）→ **备选**：FTS5 用 trigram 分词，效果稍差但零额外依赖

---

### Phase 2 — 在线同步（只读）

| 项 | 说明 |
|---|---|
| **预估** | 1-2 周 |
| **目标** | 脱离"先 export 再 import"的折腾 |

**任务清单**：
- [ ] **Token 管理**：设置页填 `pku_token` / `pku-uuid`；401 时弹窗提示
- [ ] **API client**：把油猴脚本的 `followedHoles` / `comments` / `getHole` 4 个端点搬入；重试、限速、超时（参考现有 `fetchJsonWithRetry`）
- [ ] **同步引擎**（核心）：
  - 拉关注列表 → diff 出 added / removed
  - 对本地已有洞：若 `reply` 数变化则重拉评论
  - 用户触发、可中断、显示进度
  - **必须幂等**：反复同步同一份数据不重复、不破坏本地标签/笔记
- [ ] **单洞抓取**：输入 pid 抓取（不在关注列表的也能存）
- [ ] **同步状态可视化**：每个洞显示"最后同步时间"、"陈旧"标识

**完成判据**：可以完全不用油猴脚本，app 自己维持数据更新。

**风险**：
- 同步过程中关注列表本身在变化（用户在官网增删）→ **对策**：以本地为准，远端 unfollow 的洞**不在本地删**（数据是用户的资产）

---

### Phase 3 — 知识库化

| 项 | 说明 |
|---|---|
| **预估** | 2-3 周 |
| **目标** | 从"洞的存档"升级为"自己的树洞知识图谱" |

**任务清单**：
- [ ] **引用图谱**：扫所有洞文/评论里的 `#pid`（5-7位数字），建引用表；洞详情页"被哪些本地洞引用"
- [ ] **图片本地化**：同步时下载图片，按 content-hash 命名去重；URL 重写指向本地
- [ ] **笔记反向链接**：`[[pid:123]]` 自动建反向索引；洞详情页"我的哪些笔记提到了这个洞"
- [ ] **导出**：
  - 单洞 Markdown（含图、含评论、含我的笔记）
  - 整库 HTML 快照（单文件、可搜索、可分享给朋友只读）
- [ ] **统计页**：年度风格（关注变化、最常用标签、活跃时段、引用 TOP）

**完成判据**：朋友看到这个 app 主动问"怎么也能装一个"。

---

### Phase 4 — 智能搜索 / 问答

| 项 | 说明 |
|---|---|
| **预估** | 1-2 周（云 API）/ 3-4 周（本地模型） |
| **目标** | 解决"想不起原话也能找回"的场景 |

**任务清单**：
- [ ] **Embedding 生成**：增量给每条洞生成向量；OpenAI 兼容接口
- [ ] **向量索引**：sqlite-vec 存储 + ANN 查询
- [ ] **混合搜索**：FTS 关键词召回 + 向量召回 + rerank 合并
- [ ] **RAG 问答**：选择知识范围（标签/收藏夹/全部）→ 提问 → LLM 回答 + 原洞引用
- [ ] **设置**：API base / key / model 名（OpenAI 兼容协议，可对接任何家）

**取舍**：数据量没上来不要做。几百条洞 FTS 就够。

**完成判据**：能用自然语言问"去年关于复试英语口都讨论了啥"，得到带引用的总结。

---

### Phase 5 — 写操作 / 完整替代

| 项 | 说明 |
|---|---|
| **预估** | 2-4 周 |
| **目标** | 完全脱离官网页 |

**任务清单**：
- [ ] **关注/取关**：API 已有，接 UI（批量取关时强制二次确认）
- [ ] **发洞 / 回复**：
  - **草稿系统**（最重要）：所有内容先存 `write_drafts` 表，明确点"发送"才走 API
  - **二次确认 modal**
  - **幂等键**（`idempotency_key` UNIQUE，重试不会发两次）
  - **失败保留草稿**，不直接消失
  - **Dry-run 模式**（假装发送只 log），开发期默认开
- [ ] **首页时间线**：拉新洞 API（需新逆向）
- [ ] **通知**：进 app 时拉一次，不后台轮询
- [ ] **风控防御**：写操作前随机 sleep；日写次数上限提示

**关键风险**：这是**唯一可能影响真实学号**的阶段。所有功能先 dry-run 跑通再切真发。

**完成判据**：可以一周不打开官网。

---

## 9. 风险与对策

| 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|
| 官方 API 字段变动 | 中 | 高（功能瘫痪） | API 层做版本兼容；解析失败保留原始 JSON 不丢数据 |
| 风控限流 | 低（自用频率下） | 中 | 默认低限速；所有同步用户触发；不做后台轮询 |
| Token 频繁过期 | 中 | 低（影响体验） | 401 弹窗 + 一键重新粘贴；保留旧数据可读 |
| 写操作误发 | 低 | **高**（社交事故） | Phase 5 强制草稿 + 二次确认 + 幂等键 |
| SQLite 损坏 | 极低 | 高 | 每次启动 backup 一份；设置页一键备份 |
| Schema 演进失误 | 中 | 高 | 严格 migration 编号；上线前用真实数据 dry-run |
| 朋友安装门槛 | 中 | 低（影响传播） | GitHub Release 出安装包；写一份新手 README |

---

## 10. 关键取舍说明

### 10.1 为什么不跳过 Phase 1 直接做 Phase 2
诱惑是"反正最终要内置同步，干嘛先做导入"。但 Phase 1 的真正价值是**强迫自己用 1-2 个月**，会暴露大量 UI / 数据模型问题。这些问题等 Phase 2 完成才发现就是返工。

**用油猴当数据源跑 1-2 个月，比任何架构评审都管用**。

### 10.2 Phase 3 vs Phase 4 先做哪个
取决于数据量：
- 关注 **1000+ 洞** → Phase 4 优先（搜不到的痛感强）
- 关注 **几百洞** → Phase 3 优先（知识图谱才有意义）

### 10.3 Phase 5 永远最后
哪怕某阶段觉得"差不多就剩发洞了快做完吧"，也压住。**读功能的 bug 是体验问题，写功能的 bug 是社交事故**——这两个不是一个量级。

### 10.4 永远不做
- 自建后端、共享数据库
- 后台常驻轮询
- 多账号 / 自动登录
- 给非熟人用、上架商店
- 任何"识别洞主身份"的功能

---

## 11. 起步指南（接下来 4 天）

| 天 | 任务 | 交付物 |
|---|---|---|
| Day 1 上午 | Tauri + React + TS 模板拉起，跑通 hello world | `npm run tauri dev` 能开窗口 |
| Day 1 下午 | 装 tauri-plugin-sql；写 `migration_001.sql`；做一个"建库"按钮 | 点击后磁盘上出现 .db 文件 |
| Day 2 | 写"导入油猴 JSON"功能，最简版（选文件→解析→insert） | 能导入一份导出文件，DB 里有数据 |
| Day 3 | 最简陋的洞列表 + 详情页（丑没关系，能看） | 能浏览导入的数据 |
| Day 4 | 加上 FTS 搜索框（先用 LIKE 也行，FTS 后补） | 能搜到关键词 |

**4 天结束**：一个虽丑但**已经能替代官方收藏夹查阅历史洞**的玩意。从这里开始迭代。

---

## 附录 A：现有油猴脚本盘点

文件：`PKU-Hole export tool.user.js`（v1.2.1，已修复 7 个问题）

**可直接复用到客户端的部分**：
- `fetchJsonWithRetry` —— 重试/超时/退避封装
- `_authHeaders` —— 鉴权头工厂
- `followedHoles` / `comments` / `getHole` / `followHole` —— 4 个 API 调用
- `extractPidFromText` —— PID 提取正则
- `comment2text` —— 评论拍平（导出用，客户端可作为可选项）

**需要丢弃 / 重写的部分**：
- `downloadFile` —— 桌面 app 用 Tauri fs API
- `processExtract` —— 移到 sync 引擎
- 所有 alert / confirm / prompt —— UI 重做
- `initInputElement` / 油猴按钮注入 —— 不再需要

---

## 附录 B：已知树洞 API 端点

| 用途 | 方法 | URL | Phase |
|---|---|---|---|
| 关注列表 | GET | `/api/follow_v2?page=&limit=25` | 2 |
| 评论列表 | GET | `/api/pku_comment_v3/{pid}?page=&limit=15&sort=asc` | 2 |
| 单洞详情 | GET | `/api/pku/{pid}/` | 2 |
| 关注洞 | POST | `/api/pku_attention/{pid}` | 5 |
| 取关 | DELETE | `/api/pku_attention/{pid}` | 5 |
| 首页时间线 | GET | （需逆向） | 5 |
| 发洞 | POST | （需逆向） | 5 |
| 回复 | POST | （需逆向） | 5 |

鉴权要素：
- `Authorization: Bearer ${pku_token}`（来自 cookie）
- `uuid: ${pku-uuid}`（来自 localStorage）

---

## 附录 C：Schema 迁移示例

```typescript
// src/db/migrations/index.ts
export const migrations = [
  { version: 1, sql: import('./001_init.sql?raw') },
  { version: 2, sql: import('./002_add_embeddings.sql?raw') },
  // ...
];

export async function migrate(db: Database) {
  const current = await db.select<{user_version: number}>(
    'PRAGMA user_version'
  );
  for (const m of migrations) {
    if (m.version > current.user_version) {
      await db.execute(await m.sql);
      await db.execute(`PRAGMA user_version = ${m.version}`);
    }
  }
}
```

---

## 附录 D:版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-06-27 | 初稿 |

---

*本文档随实现进展持续更新。任何与文档冲突的实现决定，应**先改文档再改代码**。*
