# PKU Treehole Toolkit —— 完整方案设计

| 字段 | 值 |
|---|---|
| 文档版本 | v1.0 |
| 日期 | 2026-07-11 |
| 状态 | 已决策 / 可实施 |
| 目标用户 | 作者本人及少量可信朋友 |
| 目标平台 | 浏览器用户脚本 + Windows / macOS / Linux 桌面端 |
| 目标数据量 | 约 2,000 个洞及其评论，预留至 10,000 个洞 |

## 0. 结论

项目采用“用户脚本采集 + Tauri 本地归档”的混合架构：

- **用户脚本**贴近官网，复用当前登录态，负责安全导出、范围同步和账号关注迁移。
- **桌面客户端**持有长期 SQLite 数据库，负责阅读、全文搜索、多标签、笔记、备份和可选 AI。
- 两端只通过版本化的 `.treehole.zip` 协议交换数据，不通过自建服务器共享用户数据。
- 用户脚本不演变成大型知识库；桌面客户端早期不执行发洞、回复等高风险写操作。

当前阶段的首要成果是稳定的用户脚本 1.3 和归档协议 v2。桌面端必须等协议和真实数据验证稳定后再启动。

## 1. 背景与目标

### 1.1 当前痛点

官网关注列表适合临时查看，但不适合长期知识管理：

- 一个洞难以表达多个主题归属。
- 搜索依赖原词，难以找回较久以前的内容。
- 无法为洞增加私人批注、引用关系和回顾统计。
- 洞被删除、账号迁移或接口变化时，历史内容可能丢失。
- 既有导出脚本缺少可验证完整性、断点恢复和安全写操作边界。

### 1.2 产品目标

1. 让用户能安全、完整、可恢复地导出约 2,000 个关注洞。
2. 让毕业、换号等场景中的关注迁移可预检、可审计，不误操作账号。
3. 建立用户拥有、可备份、可迁移的本地长期档案。
4. 提供明显优于官网的搜索、标签、笔记和回顾体验。
5. 保持少量朋友可安装、可升级，并在三大桌面平台保持同一数据协议。

### 1.3 非目标

- 不建设集中式后端，不托管或共享用户数据库。
- 不自动登录，不在普通配置、日志或 SQLite 中保存 token。
- 不进行后台常驻轮询；网络同步必须由用户主动触发。
- 不做洞主身份识别、跨账号画像或匿名性反向破解。
- 不在当前路线中实现桌面端发洞、回复或完整替代官网。
- 不承诺移动端；移动端需求另立项目评估。

## 2. 为什么不只做网页

### 2.1 网页方案能够完成的能力

对于约 2,000 个洞，用户脚本配合 IndexedDB/OPFS 可以实现：

- 关注列表和评论采集。
- 分组、日期和 PID 范围导出。
- 本地标签、笔记、引用关系和轻量全文索引。
- 云端 Embedding/RAG。

因此“只靠网页”在技术上可行。

### 2.2 网页作为唯一长期存储的不足

- 数据绑定浏览器配置和站点 origin，浏览器清理、重装或隐身模式会影响持久性。
- 配额、持久化授权和清理策略因浏览器而异。
- 大库原子备份、损坏恢复、文件级迁移和跨浏览器兼容较复杂。
- SQLite WASM、OPFS、Worker 和用户脚本沙箱组合后，工程复杂度接近独立应用。
- 大型本地模型、系统密钥链和可靠本地图片库不适合用户脚本。

### 2.3 最终取舍

用户脚本只把 IndexedDB 用作**短期任务 checkpoint**，成功后生成用户持有的 ZIP；不把浏览器数据库当长期档案真源。长期数据进入桌面 SQLite。

独立 SPA 不采用，因为会重新遇到 CORS、登录态和存储边界问题。浏览器扩展比 SPA 更强，但对于本项目仍不能明显优于“用户脚本 + Tauri”，因此不作为第三套正式产品。

## 3. 总体架构

```text
┌──────────────────────────────┐
│ treehole.pku.edu.cn          │
│ Cookie + UUID 登录态         │
└──────────────┬───────────────┘
               │ 同源、串行、主动触发
┌──────────────▼───────────────┐
│ Userscript                   │
│ 范围规划 / API 调度 / 断点   │
│ 导入预检 / 单次关注写操作    │
└──────────────┬───────────────┘
               │ .treehole.zip v2
┌──────────────▼───────────────┐
│ Tauri Desktop                │
│ Rust 边界 / SQLite / FTS     │
│ 标签 / 笔记 / 搜索 / 备份    │
└──────────────────────────────┘
```

仓库采用 monorepo：

```text
apps/
  userscript/       # 当前可运行产品
  desktop/          # Phase 1 才创建
packages/
  archive-schema/   # v2 schema 与兼容 fixture
scripts/            # 构建和验证
tests/              # 请求、任务、归档和 UI 测试
```

根目录用户脚本是生成产物。源码、版本号和归档 schema 分别只有一个真源。

## 4. 用户脚本 1.3 设计

### 4.1 功能范围

导出范围：

- 全部关注。
- 官方收藏分组：从 `/bookmark` 读取分组，向 `/follow_v2` 传 `bookmark_id`。
- 指定 PID。
- 日期范围。
- 仅正文或正文加评论。

引用范围：

- 不抓取引用。
- 只抓正文中的 `#PID`。
- 抓正文和评论中的 `#PID`。

引用只展开一层，不递归。超过 200 个需要再次确认，硬上限为 2,000 个；超过上限的任务必须标记为部分完成。

导入支持：

- v2 `.treehole.zip`。
- 旧版 `{ holes: [], comments: [] }` JSON。
- 多文件合并、PID 去重、当前关注集预检和二次确认。
- 最终审计报告，包括成功、已关注、不存在、失败和结果未知。

### 4.2 请求策略

所有 API 请求经过同一串行调度器：

| 类型 | 策略 |
|---|---|
| GET | 并发 1；600ms + 0–300ms jitter；总尝试最多 3 次 |
| POST | 并发 1；间隔至少 1000ms；总尝试固定为 1 次 |
| 超时 | 20 秒，覆盖响应头和 JSON 读取 |
| 可重试 | 网络错误、408、429、500、502、503、504 |
| 不重试 | 400、401、403、404、其他业务错误 |
| 429 | 尊重 `Retry-After`；缺失时暂停 60 秒；第二次 429 等待手动恢复 |
| 401/403 | 立即停止任务并提示重新登录 |

关注接口具有状态切换风险。POST 响应不确定时不得重发，必须重新 GET 单洞，只有最终 `is_follow=true` 才算成功；否则记录 `unknown_result`。

### 4.3 错误模型

```text
not_found
unauthorized
rate_limited
network_error
invalid_response
business_error
unknown_result
cancelled
invalid_input
storage_error
```

只有明确的 404 或官方“不存在”业务响应可记为 `not_found`。错误记录包含 operation、HTTP status、是否可重试、PID 和阶段。

### 4.4 任务状态与恢复

```text
planning → running ↔ paused
                    ├→ completed
                    ├→ partial
                    ├→ failed
                    └→ cancelled
```

- 每完成一个洞即写 IndexedDB checkpoint。
- checkpoint 保存选项、完成 PID、分页结果、错误和临时归档项，不保存 token。
- checkpoint 绑定 UUID 的 SHA-256 指纹，不能跨账号恢复。
- 页面刷新后把遗留 `running` 任务转换为 `paused` 并展示恢复入口。
- 成功和部分成功任务保留七天，支持重新生成 ZIP；过期自动清理。

### 4.5 页面交互

- 使用 Shadow DOM，避免官网 CSS 污染。
- 入口使用唯一 ID 和幂等挂载，不覆盖 `window.onload`。
- 官网锚点延迟出现或 SPA 重绘时，由 MutationObserver 重新放置；10 秒后仍无锚点则使用右下角浮动入口。
- 设置、预检、进度和结果都在一个非阻塞面板中完成。
- 保留稳定按钮名称，使用 `aria-live`、`aria-busy` 和原生 progress 表达状态。
- 任务提供暂停、继续、取消和仅重试未完整项。

## 5. 归档协议 v2

### 5.1 文件结构

```text
pku-treehole-<runId>.treehole.zip
├── manifest.json
├── data.json
└── readable.txt       # 可关闭
```

当前 writer 使用标准 ZIP store 方法以避免把第三方压缩库带入登录态脚本；以后可以增加 deflate，但不得改变内部文件或 JSON schema。

### 5.2 manifest

必填字段：

```json
{
  "schemaVersion": 2,
  "toolVersion": "1.3.0",
  "runId": "...",
  "exportedAt": "2026-07-11T00:00:00.000Z",
  "scope": {},
  "complete": true,
  "counts": {
    "expectedHoles": 2000,
    "exportedHoles": 2000,
    "comments": 12000,
    "failed": 0
  },
  "dateRange": {},
  "errors": []
}
```

只要出现分页上限、引用硬上限、单洞失败或评论不完整，`complete` 必须为 false。

### 5.3 数据项

```json
{
  "items": [
    {
      "pid": "123456",
      "source": "followed",
      "hole": {},
      "comments": [],
      "fetchStatus": "ok"
    }
  ]
}
```

- `source` 为 `followed`、`referenced`、`explicit` 或 `legacy-v1`。
- 引用解析同时接受原始 `#PID`，以及关注接口在正文开头返回的无 `#` `PID + 空格` 格式；正文中部的普通长数字不得被误判。
- 评论拍平并直接与 PID 所在 item 关联，不再依赖并行数组下标。
- 为应对接口字段变化，保留 API 原始对象，但递归删除名称匹配 token、authorization、cookie、uuid 的字段。
- 归档永远不包含请求头、Cookie 字符串、原始 UUID 或可跨归档关联账号的稳定指纹。
- `accountFingerprint` 只保存在本机 IndexedDB 任务记录中，用于阻止其他账号恢复断点，不进入 ZIP。

### 5.4 兼容策略

- 用户脚本和桌面端必须同时支持 v1 导入。
- 关注导入只接受 `followed`、`explicit` 和 `legacy-v1`；`referenced` 仅为阅读上下文，禁止转换为关注写入。
- 当前账号关注分页不完整时，预检和执行层都必须拒绝写入。
- 新功能只写 v2；旧格式导出仅作为显式兼容选项保留一个大版本周期。
- 未识别的新 schema 只能只读提示，禁止猜测解析。
- 所有兼容变更必须增加匿名 fixture 和 round-trip 测试。

## 6. 桌面端 Phase 1 架构

### 6.1 技术栈

| 层 | 选择 |
|---|---|
| 应用壳 | Tauri 2.x |
| 前端 | React + TypeScript + Vite |
| UI | Tailwind CSS + 可复制维护的组件 |
| 本地状态 | Zustand |
| 异步状态 | TanStack Query |
| 数据库 | SQLite |
| 迁移 | `tauri-plugin-sql` Rust migrations |
| 搜索 | FTS5 trigram；后续可选 jieba |
| 密钥 | Tauri Stronghold / 系统密钥链 |

Phase 1 不包含网络，先验证导入、数据模型和日常阅读体验。

### 6.2 安全边界

- WebView 不获得通用 SQL、文件系统或任意 HTTP 权限。
- 前端只调用窄 Rust command，例如 `import_archive`、`search_holes`、`save_note`、`create_backup`。
- 所有命令验证路径、PID、查询条件和输入大小。
- Tauri capabilities 只授予当前窗口需要的命令；CSP 禁止远端脚本和任意导航。
- 洞文和评论默认作为纯文本渲染。Markdown 禁止原始 HTML，并通过 allowlist sanitizer。
- HTML 快照按 HTML、属性和 URL 上下文分别转义，内置 CSP，默认不加载远端图片。

## 7. SQLite 数据模型

### 7.1 原则

1. 远端同步数据、本地用户数据和原始导入记录分层保存。
2. 重新导入或同步不得覆盖标签、笔记和本地归档状态。
3. 所有批量导入先进入 staging/raw，再在单事务中写正式表。
4. 每个连接显式执行并验证 `PRAGMA foreign_keys=ON`。

### 7.2 核心表

```sql
CREATE TABLE accounts (
  id TEXT PRIMARY KEY,
  fingerprint TEXT NOT NULL UNIQUE,
  created_at INTEGER NOT NULL
);

CREATE TABLE holes (
  pid INTEGER PRIMARY KEY,
  text TEXT NOT NULL,
  timestamp INTEGER NOT NULL,
  likenum INTEGER NOT NULL DEFAULT 0,
  reply INTEGER NOT NULL DEFAULT 0,
  type TEXT,
  image_url TEXT,
  content_hash TEXT NOT NULL,
  raw_json TEXT,
  first_seen_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

CREATE TABLE comments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  pid INTEGER NOT NULL REFERENCES holes(pid) ON DELETE CASCADE,
  remote_cid INTEGER,
  name TEXT,
  text TEXT NOT NULL,
  timestamp INTEGER,
  reply_to_cid INTEGER,
  floor INTEGER,
  raw_json TEXT,
  UNIQUE(pid, remote_cid)
);
CREATE INDEX idx_comments_pid ON comments(pid);

CREATE TABLE remote_follows (
  account_id TEXT NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
  pid INTEGER NOT NULL REFERENCES holes(pid) ON DELETE RESTRICT,
  is_followed INTEGER NOT NULL,
  first_seen_at INTEGER NOT NULL,
  last_seen_at INTEGER NOT NULL,
  bookmark_id TEXT,
  PRIMARY KEY(account_id, pid)
);

CREATE TABLE tags (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL COLLATE NOCASE UNIQUE,
  color TEXT,
  created_at INTEGER NOT NULL
);

CREATE TABLE hole_tags (
  pid INTEGER NOT NULL REFERENCES holes(pid) ON DELETE CASCADE,
  tag_id INTEGER NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
  PRIMARY KEY(pid, tag_id)
);

CREATE TABLE notes (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  pid INTEGER REFERENCES holes(pid) ON DELETE SET NULL,
  content TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

CREATE TABLE references_graph (
  source_pid INTEGER NOT NULL REFERENCES holes(pid) ON DELETE CASCADE,
  target_pid INTEGER NOT NULL,
  source_type TEXT NOT NULL,
  source_id INTEGER,
  PRIMARY KEY(source_pid, target_pid, source_type, source_id)
);

CREATE TABLE sync_jobs (
  id TEXT PRIMARY KEY,
  account_id TEXT REFERENCES accounts(id),
  type TEXT NOT NULL,
  state TEXT NOT NULL,
  options_json TEXT NOT NULL,
  cursor_json TEXT,
  total INTEGER,
  completed INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

CREATE TABLE sync_job_items (
  job_id TEXT NOT NULL REFERENCES sync_jobs(id) ON DELETE CASCADE,
  pid INTEGER NOT NULL,
  state TEXT NOT NULL,
  error_json TEXT,
  PRIMARY KEY(job_id, pid)
);

CREATE TABLE raw_records (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  batch_id TEXT NOT NULL,
  source TEXT NOT NULL,
  external_id TEXT,
  payload_json TEXT NOT NULL,
  parser_version TEXT NOT NULL,
  parse_error TEXT,
  created_at INTEGER NOT NULL
);
```

### 7.3 FTS

```sql
CREATE VIRTUAL TABLE holes_fts USING fts5(
  text,
  content='holes',
  content_rowid='pid',
  tokenize='trigram'
);

CREATE VIRTUAL TABLE comments_fts USING fts5(
  text,
  content='comments',
  content_rowid='id',
  tokenize='trigram'
);
```

迁移同时创建 INSERT、UPDATE、DELETE 三类同步触发器。创建触发器后必须执行一次 `rebuild`，并提供 FTS integrity check。若目标 SQLite 不支持 trigram，启动自检失败后回退到 `unicode61` + LIKE，不允许整个数据库初始化失败。

## 8. 同步设计

Phase 2 才增加只读网络同步：

1. token 和 UUID 由用户在设置页填写，保存在 Stronghold；Rust 使用后只短暂留在内存。
2. 拉关注列表快照并与 `remote_follows` diff。
3. 新关注洞进入抓取队列；远端取关只更新 `is_followed=0`，不删除本地资产。
4. 已有洞依据内容 hash、回复数、TTL 和用户强制刷新决定是否重拉。
5. 单洞更新先写 staging，在事务中替换洞和评论；失败保留旧版本。
6. 所有同步记录 job、cursor、单项状态和错误，可暂停、恢复和只重试失败项。

同步客户端复用用户脚本的错误语义，但不复用 Cookie 解析和浏览器请求头。HTTP 必须由 Rust 或 Tauri native HTTP 完成，host scope 固定为 `https://treehole.pku.edu.cn/api/*`。

## 9. 备份与数据生命周期

- 使用 SQLite Online Backup API 或 `VACUUM INTO` 创建一致性快照，禁止在数据库运行时直接复制主文件。
- 默认保留最近 7 个日备份和 4 个周备份，超出后轮转。
- 备份前执行 `foreign_key_check`，定期执行 `quick_check` 和 FTS integrity check。
- 数据库、图片和 manifest 使用同一个 backup runId，避免跨版本错配。
- 设置页提供整库导出、恢复演练、删除本地数据和打开数据目录。
- 默认数据库不声称加密；文档明确依赖操作系统账号和磁盘保护。需要跨设备加密时另行设计，不自创密码学方案。

## 10. AI 与数据外发

AI 是可选 Phase 4，不是 MVP 依赖：

- 默认关闭，优先支持本地模型。
- 云 provider 必须使用 HTTPS allowlist；API key 保存到 Stronghold。
- 每次首次处理某个范围前，展示 provider、模型、将发送的洞数、字段和是否包含私人笔记。
- 用户可以只选择指定标签/收藏夹，默认不发送笔记和原始 JSON。
- Embedding 以 chunk 存储，包含模型、维度、内容 hash 和生成时间；内容变化后只重算受影响 chunk。
- RAG 回答必须提供本地洞引用，不把模型生成内容写回原始档案。

## 11. 路线图与验收

### Phase 0：用户脚本 1.3

交付：安全请求、范围导出、v2 ZIP、导入预检、任务恢复、Shadow DOM 面板、自动测试和 beta 发布。

验收：

- POST 在任何错误下注定只发送一次。
- 401/403 不重试，第二次 429 暂停。
- 2,000 洞 mock 导出无重复、可取消并恢复。
- 完整任务 manifest 计数一致；任意失败只能得到 partial/failed。
- v1 JSON 和 v2 ZIP 均能预检并导入。
- 初始化只执行一次，SPA 重绘后入口仍唯一。

### Phase 1：离线桌面归档

交付：Tauri 三平台骨架、v1/v2 导入、洞列表/详情、标签、笔记、FTS 搜索、备份恢复。

开工前 spike：

1. 三平台安装包和签名/公证成本。
2. 真实 2,000 洞数据上的 trigram 搜索质量与延迟。
3. v1/v2 多文件导入事务和失败回滚。

验收：

- 导入同一归档两次结果幂等，不重复评论或标签。
- 10,000 洞 fixture 搜索 p95 小于 200ms。
- 标签和笔记不被重新导入覆盖。
- 备份恢复后行数、hash、外键和 FTS 检查通过。
- 键盘可完成导入、搜索、标签和笔记流程。

### Phase 2：主动只读同步

交付：Stronghold 凭证、只读 API、增量同步、单洞刷新、同步 checkpoint 和诊断包。

验收：

- token 不出现在前端、日志、SQLite、备份和崩溃报告。
- 同一同步任务重复运行幂等。
- 中途退出后恢复不重复写入。
- 远端取关不删除本地洞。

### Phase 3：知识库增强

交付：引用图、图片本地化、反向链接、统计、Markdown 和安全 HTML 导出。

验收：

- 图片只允许已配置的树洞/CDN HTTPS host，限制大小、MIME 和重定向。
- HTML 快照离线打开时不联网，不执行洞文中的 HTML/脚本。
- 引用图和笔记反链可以从正式表完全重建。

### Phase 4：可选 AI

交付：本地/云 Embedding、混合搜索、限定范围 RAG。

验收：

- 没有用户授权时无任何内容外发。
- 内容变化后只重算对应 chunk。
- 所有回答包含可打开的本地引用。

## 12. 测试与发布

### 12.1 自动测试

- 请求：401、403、429、5xx、超时、响应解析失败、POST 响应丢失。
- 导入：路径/query PID、坏 JSON、重复 PID、超大文件、ZIP CRC、旧格式。
- 导出：零评论、多页评论、分页重复、上限截断、引用去重、部分失败。
- 状态：暂停、取消、刷新恢复、账号指纹不匹配、只重试未完整项。
- UI：入口唯一、不覆盖 onload、延迟 DOM、SPA 重挂载、ARIA 状态。
- 协议：v1 adapter、v2 round-trip、敏感字段剔除、manifest 计数。

### 12.2 用户脚本发布

1. GitHub main 通过 CI，生成脚本无 diff。
2. 发布 `1.3.0-beta.N` 到独立 GreasyFork beta ID。
3. 只用 5–10 个洞的小分组验证真实 API，不进行压力测试。
4. 验证 Chrome/Edge/Firefox 与 Tampermonkey/Violentmonkey。
5. 打 GitHub `v1.3.0` tag，Release 附脚本、SHA-256、changelog 和 schema。
6. 更新 GreasyFork stable，再回读远端版本和脚本 hash。

稳定构建只指向稳定渠道；beta 和 stable 使用不同 update URL。创建正式 GreasyFork ID 前，构建不继承旧项目的自动更新 URL。

### 12.3 桌面端发布

- Windows：安装包、卸载和 WebView2 最低版本测试。
- macOS：Intel/Apple Silicon、签名和公证。
- Linux：明确 WebKitGTK 最低依赖，至少验证一个 Debian/Ubuntu 和一个滚动发行版。
- 数据库 schema 和 archive schema 独立版本化；应用降级时不得自动降级数据库。

## 13. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 官方 API 或 DOM 改动 | 脚本不可用 | API 响应校验、DOM fallback、beta 回读、原始记录隔离 |
| 请求限流 | 账号短期受限 | 并发 1、范围导出、缓存、429 熔断、禁止自动压力测试 |
| 归档部分失败 | 用户误认为已备份 | manifest、complete 标志、失败清单、checkpoint |
| 关注写操作歧义 | 错误账号状态 | POST 一次、GET 对账、unknown_result 人工处理 |
| 浏览器清理 IndexedDB | checkpoint 丢失 | checkpoint 仅作短期恢复，ZIP 才是交付物 |
| SQLite 迁移失败 | 本地库损坏 | Rust 原子迁移、升级前备份、真实旧库测试 |
| WebView stored XSS | 本地数据/系统能力暴露 | 纯文本、sanitizer、CSP、最小 capabilities、窄命令 |
| 云 AI 数据外发 | 隐私泄露 | 默认关闭、范围预览、显式同意、Stronghold、host allowlist |
| 跨平台安装门槛 | 朋友无法使用 | Phase 1 先做安装 spike、签名、公证和明确依赖 |

## 14. 已确定的架构决策

1. 当前项目保留用户脚本，不直接跳到桌面端。
2. 用户脚本和桌面端处于同一 monorepo，共享 archive schema。
3. 浏览器只保存七天 checkpoint，不作为长期数据库。
4. 默认导出一个 v2 ZIP，旧格式仅兼容导入。
5. 用户脚本不使用远程 `@require`，构建产物可独立审计。
6. Tauri Phase 1 完全离线，Phase 2 才增加只读同步。
7. 完整替代官网和桌面端写操作不在当前路线中。
8. AI 是显式授权的可选层，不改变本地优先原则。

任何改变以上决策的实现必须先提交 ADR 并更新本文档，再修改代码。
